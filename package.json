{
  "name": "ethio-home",
  "version": "1.0.0",
  "private": true,
  "description": "Plugin-driven Home Assistant dashboard platform",
  "scripts": {
    "dev": "pnpm --filter @ethio/dashboard dev",
    "build": "pnpm --filter @ethio/dashboard-schema build && pnpm --filter @ethio/ha-sdk build && pnpm --filter @ethio/plugin-sdk build && pnpm --filter @ethio/core build && pnpm --filter @ethio/teamtracker build && pnpm --filter @ethio/sinksar build && pnpm --filter @ethio/dashboard build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r --if-present test",
    "preview": "pnpm --filter @ethio/dashboard preview"
  },
  "packageManager": "pnpm@10.9.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ],
    "patchedDependencies": {
      "expo-widgets@57.0.15": "patches/expo-widgets@57.0.15.patch"
    }
  }
}
